import styled from 'styled-components';

const Window = (styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  height:        100%;
  width:         100%;
  border-radius: 15px;
`);

export default {
  Window
}